{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "TimeTracker": "Server=localhost;Database=TimeTracker;Trusted_Connection=True;TrustServerCertificate=True;"
  },
  "Auth": {
    "//": "Dev backdoor: authenticate as a stand-in user without a real login. Set UseDevBypass=false to test real JWT locally.",
    "UseDevBypass": true,
    "DevUser": {
      "Sub": "dev-local-user",
      "Email": "dev@local",
      "Name": "Dev User",
      "Admin": true
    }
  }
}
