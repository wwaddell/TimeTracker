{
  "//": "Dev-only API base. Used when running the WASM client via the Blazor DevServer on :5008, which is cross-origin to the API at :5130. In hosted dev (the API serves the WASM at :5130), the standard appsettings.json absence triggers HostEnvironment.BaseAddress instead — so this value is harmless but unused.",
  "ApiBaseUrl": "http://localhost:5130"
}
